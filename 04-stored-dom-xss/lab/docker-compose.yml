name: 04-stored-dom-xss
services:
  lab:
    build: .
    image: 04-stored-dom-xss:latest
    container_name: 04-stored-dom-xss
    # Publish on the host loopback only - never exposed to the network.
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
